import { Text, View } from 'react-native';

export default function LiveTracking() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Live Tracking / Notifications Screen</Text>
    </View>
  );
}
